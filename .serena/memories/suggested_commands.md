# Suggested Commands
- `docker-compose up --build` – build and run the Flask app with Nginx reverse proxy.
- `python app/app.py` – run the Flask development server directly.
- `pip install -r app/requirements.txt` – install Python dependencies for the Flask app.
- `pytest` – no test suite present yet; run once tests are added.